FROM python:3.12-slim

WORKDIR /app

# Install only the deps needed for server (no pynput/pystray/Pillow/psutil)
COPY requirements-server.txt .
RUN pip install --no-cache-dir -r requirements-server.txt

COPY trackyr/ trackyr/

EXPOSE 8099

CMD ["python", "-m", "trackyr.server"]
